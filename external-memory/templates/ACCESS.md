# ACCESS registry

Pointers only, never values. Each row says where the credential lives. The linter blocks token shapes; if you are about to paste a value, stop and add a pointer instead.

<!-- "Where it lives" names the env var, profile, file path, or vault item. Machine-specific paths and quirks go in ACCESS.local.md (gitignored), never here. -->

| Service | Where it lives | Notes |
| --- | --- | --- |
